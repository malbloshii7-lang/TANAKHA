"""WMO Presidential Campaign Portal — Phase 1 (team-only, bilingual PWA).

Self-contained package: its own SQLite tables, stateless auth, and API router.
Mounted onto the main FastAPI app so one deploy serves both the existing site
and the campaign portal.
"""
